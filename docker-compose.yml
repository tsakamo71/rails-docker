version: "3"

services:
  app:
    build: .
    tty: true
    volumes:
      - ./app:/app
    links:
      - postgresql
    environment:
      POSTGRE_SQL_HOST: postgresql
  postgresql:
    image: 'postgres:11.10'
    environment:
      POSTGRES_USER: 'root'
      POSTGRES_PASSWORD: 'password'

